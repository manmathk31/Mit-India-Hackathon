server.port=8082

# Optional. The cost calculation itself does NOT depend on the LLM.
# Set this only if you want the Spring AI explanation endpoint later.
spring.ai.vertex.ai.gemini.api-key=${GOOGLE_API_KEY:}
spring.ai.vertex.ai.gemini.chat.options.model=gemini-2.5-flash
